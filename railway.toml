[build]
builder = "NIXPACKS"
buildCommand = "cd apps/web && npm ci && npm run build"
watchPatterns = ["apps/web/**", "data/imports/**"]

[deploy]
startCommand = "cd apps/web && npm run db:migrate && npm run db:import-guests && npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.healthcheck]
path = "/borneo"
timeout = 300
