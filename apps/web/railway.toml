[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run db:migrate && npm run db:import-guests && npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.healthcheck]
path = "/borneo"
timeout = 300
